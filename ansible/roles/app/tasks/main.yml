---
# application tasks to be customized and to run after the main provision
- name: Install Odoo
  sudo: yes
  shell: curl https://raw.githubusercontent.com/aschenkels-ictstudio/odoo-install-scripts/master/ubuntu-14-04/odoo_install.sh > /root/odoo_install.sh && sh /root/odoo_install.sh
